import:
  - logstash-plugins/.ci:travis/travis.yml@1.x

env:
  - DISTRIBUTION=default ELASTIC_STACK_VERSION=7.x
  - DISTRIBUTION=default ELASTIC_STACK_VERSION=7.x SNAPSHOT=true
  - DISTRIBUTION=default ELASTIC_STACK_VERSION=7.x INTEGRATION=true
  - DISTRIBUTION=default ELASTIC_STACK_VERSION=7.x INTEGRATION=true SNAPSHOT=true
  - DISTRIBUTION=default ELASTIC_STACK_VERSION=8.x DOCKER_ENV=dockerjdk17.env
  - DISTRIBUTION=default ELASTIC_STACK_VERSION=8.x DOCKER_ENV=dockerjdk17.env SNAPSHOT=true
  - DISTRIBUTION=default ELASTIC_STACK_VERSION=8.x DOCKER_ENV=dockerjdk17.env INTEGRATION=true
  - DISTRIBUTION=default ELASTIC_STACK_VERSION=8.x DOCKER_ENV=dockerjdk17.env INTEGRATION=true SNAPSHOT=true